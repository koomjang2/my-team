# My Team — 애터미 실질 직급 계보도

## 세션 인계
사용자가 "작업파일 이어서 하자" 또는 이와 비슷하게(예: "이어서 하자", "지난 작업 이어서")
말하면, 먼저 [HANDOFF.md](HANDOFF.md) 를 읽고 거기 적힌 최근 커밋·진행 상황·답을
기다리던 질문부터 이어간다. 다른 컴퓨터에서 새로 클론했을 때를 위한 것이므로
`git log` 로 실제 최신 커밋과 HANDOFF.md 의 내용이 맞는지도 함께 확인할 것.

## 프로젝트 개요
애터미 사업자가 **보름(반기) 단위로 직급을 맞추기 위한 실질 계보도를 설계**하는 웹앱.
"내가 SRM이 되려면 내 계보도에서 누가 DM을 가고 누가 SM을 가야 하는가"를 짜보는 도구다.

`atomy-simul` 의 조직트리 UI(팬/줌/이진 배치)를 응용해 만들었으나, 별도 저장소이며
날짜별 PV 배분 최적화 엔진은 가져오지 않았다. 이 앱은 **자동 최적화가 아니라 구조 설계**가 목적이다.

## 직급이 세 종류다 (헷갈리기 쉬움 — 반드시 구분할 것)
| 개념 | 필드 | 뜻 | 어디에 |
|---|---|---|---|
| **명목 직급** | `node.nominalRank` | 그 회원이 이름표로 달고 있는 직급 | 왼쪽 카드 · 팝오버 상단 9개 버튼 |
| **달성할 직급** | `node.rank` | 이번 보름에 맞추려는 목표 | 왼쪽 카드 · 팝오버 '목표 직급 선택' 10종 |
| **실질 직급** | 계산값 | 조건을 실제로 만족한 직급 | 오른쪽 패널 |

### 카드 색이 따르는 직급 (헷갈리기 쉬움)
| 패널 | 카드 색 | 카드에 적히는 직급 |
|---|---|---|
| 좌 · 나의 계보도 | **명목 직급** | `[명목] ○○` 와 `[목표] ○○` 두 줄 |
| 우 · 목표 계보도 | **달성할 직급** | 달성할 직급 (뱃지 없음) |

### 회원 ID 보이기 — 패널마다 따로, 기본값이 서로 반대
두 패널 머리줄에 각각 `ID` 체크박스가 있다 (말풍선은 '회원 ID 보이기/감추기'). 상태는 `App.jsx` 의
`showOrgIds` · `showEffectiveIds` 이고 `localStorage['my-team-ui-v1']` 에
`menuOpen`·`summaryOpen` 과 함께 저장된다.

| 패널 | 기본값 | 저장값이 없을 때 읽는 법 |
|---|---|---|
| 좌 · 나의 계보도 | **켜짐** — 내가 짜는 화면이다 | `!== false` |
| 우 · 목표 계보도 | **꺼짐** — 남에게 보여줄 때가 있다 | `=== true` |

**읽는 방향이 서로 반대다.** 한쪽을 고치면서 다른 쪽 방식을 따라가면 기본값이 뒤집힌다.

두 값과 함께 `splitPctX`(가로 배치일 때 왼쪽 패널 너비 %)도 같은 칸에 저장된다.
상하 비율(`splitPct`)은 그때그때 화면에 맞추는 값이라 기억하지 않는다.

이 두 값은 파일 저장 시 `payload.ui` 에 함께 담긴다 — 계보도 데이터가 아니라
보기 취향이지만, PV 최적화 시뮬레이터가 이 파일을 열 때 ID 를 보일지 정하는 데 쓴다.
(`atomy-simul/src/engine/importMyTeam.js` 의 `wantsIds()`)

왼쪽 카드는 직급 이름 **왼쪽**에 `명목` / `목표` 뱃지를 붙여 어느 쪽 직급인지 밝힌다.
('목표' 는 `node.rank` = 달성할 직급이다. 예전 `실질/예비` 사업자 뱃지를 대체했다.)

셋은 서로 독립이다. 예: **명목 STM 인데 이번 보름은 실질 SRM** 인 경우가 정상이다.
명목 직급은 계산에 전혀 쓰지 않는다 — 순수 표시용이다.

**실질 직급은 카드에 적지 않는다.** 오른쪽 '목표 계보도' 는 짜 놓은 그림을 보는
화면이라, 조건을 못 채웠어도 고른 목표 직급이 이름·색 그대로 남는다. 실질 직급은
못 채운 카드 아래의 붉은 부족분 문구와 머리줄 요약(`실질 ○○`, 직급별 인원)에만 나온다.

### 화면에 쓰는 이름 (코드 주석의 개념어와 다르다)
| 개념어 (문서·코드) | 화면 표기 |
|---|---|
| 계보도 구성 패널 | **나의 계보도** |
| 실질 직급 계보도 패널 | **목표 계보도** |
| 달성할 직급 (`node.rank`) | **목표 직급** · 카드 뱃지는 `목표` |

### 선택지 구성
- 명목 직급: `없음` + SSM~IM = **9개** (소비자 CSM 없음)
- 달성할 직급: `없음` + `CSM` + SSM~IM = **10개**

### 카드에서 목표 직급을 고르는 두 가지 길
왼쪽 카드의 `[목표] ○○` 는 둥근 상자 + 아래 화살표가 붙은 **버튼**이다.
- **목표 상자를 누르면** → 직급 목록만 바로 뜬다 (`RankQuickPicker.jsx`). 큰 편집창은 안 열린다.
- **카드의 다른 곳을 누르면** → 예전처럼 편집창(`NodeEditorPopover.jsx`)이 열린다.

둘은 한 번에 하나만 뜬다 (`OrgTreePanel` 의 `editingId` / `pickingRankId`).

### 단축키 (`keyboard.js`)
회원 하나가 열려 있는 동안(편집창 **또는** 직급 목록)만 듣는다. 아무것도 안 열렸으면 무시한다.

| 키 | 하는 일 |
|---|---|
| `esc` | 닫기 — 글자 칸에 커서가 있어도 듣는다 |
| `enter` | 글자 칸(이름·ID·PV)에서 입력을 끝내고 커서를 뺀다. 편집창은 안 닫힌다 |
| `` ` `` `1`~`8` | 목표 직급 (CSM · SSM~IM). `없음` 에는 단축키가 없다 |
| `Q` / `W` | 좌 · 우 하위 추가 — 자리가 차 있으면 기존 회원 위에 끼워 넣는다 |

- **esc 를 뺀 나머지는 커서가 글자 칸(이름·ID·PV·메모)에 있으면 듣지 않는다.**
  이름에 `2` 를 칠 수 있어야 하기 때문이다.
- **엔터가 그 사이를 끊어 준다** (`commitOnEnter`). 이름을 다 치고 엔터를 누르면
  커서가 빠져 바로 `` ` `` `1`~`8` 로 목표 직급을 고를 수 있다. 이름·회원 ID·PV 칸에
  붙어 있고, **메모는 예외다** — 거기서 엔터는 줄바꿈이어야 한다.
  한글 조합 중의 엔터는 조합을 확정하는 키라 흘려보낸다 (`isComposing` · `keyCode 229`).
  React 합성 이벤트에는 `isComposing` 이 없어 `e.nativeEvent` 에서도 함께 본다.
- 키는 `e.code` 로 먼저 보고 안 되면 `e.key` 로 본다 — 한글 입력 상태에서 `q` 가
  `ㅂ` 로 바뀌어도 자판의 물리 위치는 그대로라 단축키가 살아 있다.
- 단축키 글자는 목록·버튼에 그대로 적혀 있다 (`RANK_HOTKEY`).

### 글자 칸 밖을 누르면 커서가 빠진다
`usePanZoom.js` 의 `blurOutside()`. 팬을 시작할 때 mousedown 의 기본 동작을 막는데
(`preventDefault`), 브라우저가 원래 그때 해 주던 blur 까지 함께 막혀 커서가 칸에
남아 있었다. 그래서 누를 때마다 직접 빼 준다 — 누른 곳이 그 칸 자신이면 그대로 둔다.
**팬의 `preventDefault` 를 손대면 이 동작이 같이 깨진다.**

### 삭제 — 그 회원만 지운다. **하위의 좌/우는 절대 바뀌지 않는다**
× 를 누르면 누른 회원만 사라진다 (`App.jsx` 의 `handleRemove`). 이 한 줄이 규칙의 전부다:
> **상위 회원이 없어져도 하위 회원들의 좌/우는 바뀌지 않는다.**

```
  A ─좌─ B                    A ─좌─ B
    └우─ C ─좌─ D ─F    →       └우─ (빈 자리) ─좌─ D ─F
           └우─ E ─G                            └우─ E ─G
```
하위가 **둘**이면 자리를 남기고 사람만 비운다 (`vacate()` → `node.vacated`).
화면에서는 카드가 사라지고 선만 지나가 A 밑이 세 갈래로 **보이지만**, 실제로는
B 가 좌 · D·E·F·G 가 **모두 A 의 우 레그**다. 빈 자리는 `rank: NONE` 이라 레그
카운팅이 건너뛰고 그 아래까지 계속 세므로 **직급 계산은 달라지지 않는다.**
(검증: 위 그림에서 A 목표 DM → `좌 SM 1/2 · 우 SM 4/2`)

하위가 하나 이하면 빈 자리를 남기지 않는다 (`collapseVacated`) — 갈림길일 때만
뜻이 있기 때문이다. 하위 하나면 그 하나가 자리를 물려받고, 없으면 자리도 사라진다.
이미 있던 빈 자리도 하위가 하나로 줄면 그때 걷힌다.
오른쪽 '목표 계보도' 가 '없음' 회원을 접는 규칙(`collapseHidden`)과 같은 셈법이다.

빈 자리를 다시 채우려면 **윗 회원의 `+좌`/`+우`** 를 누른다 — 그 자리를 채울 뿐
위에 끼워 넣지 않는다 (`handleAdd` 의 `occupant.vacated` 갈래). 하위는 그대로 붙어 있다.

> 루트('나')는 삭제 버튼이 없다. 계보도 일부를 통째로 지우려면 아래에서부터 하나씩
> 지운다. 잘못 눌렀으면 되돌리기 한 번에 원래 자리까지 그대로 돌아온다.

### 계보도 이식 — 고른 자리 아래를 남의 파일로 갈아 끼운다
편집창 머리줄의 `계보도 불러오기`. 하위 사업자가 자기 계보도를 새로 짜서 보내 왔을 때,
그 자리와 **그 아래 전부**를 파일 내용으로 바꾼다. (`src/engine/subtreeImport.js`)

```
  내 계보도              A 가 준 파일        이식 결과
  나 ─우─ A ─좌─ 가       A' ─좌─ B          나 ─우─ A' ─좌─ B
                            └우─ C                    └우─ C
```

계보도가 평평한 배열이라 **파일 루트에 내 `parentId`·`side` 만 갈아 끼우면** 그 자리에
매달린다. 엔진(`rankEngine.js`)은 한 줄도 안 건드린다 — 붙는 순간 다시 계산된다.

접합점(고른 자리)에서 **내 것으로 남는 것은 셋뿐**이다:

| 남기는 것 | 왜 |
|---|---|
| `id` | 안 물려받으면 열려 있던 편집창과 `selectedId` 가 끊긴다 |
| `parentId` · `side` | 파일 루트는 `null` 이라 안 갈면 트리가 두 동강 난다 |
| `memo` | 내가 그 사람을 보고 적은 것이라 파일 메모와 **합친다**(`mergeMemo`) |

나머지(이름·회원ID·명목직급·**목표직급**·PV)는 **파일이 이긴다** — 본인이 자기를 더 잘 안다.
목표 직급까지 덮으므로 **내 실질 직급이 내려앉을 수 있다. 그것이 이 기능의 값어치다** —
A 의 실제 계획을 넣었을 때 내 직급이 어떻게 되는지가 오른쪽 패널에 바로 나온다.

**메모는 하위도 합친다.** 한때는 접합점만 합쳐졌는데, 그건 규칙이 아니라 접합점만 id 를
물려받아 살아남았기 때문이었다 — 나머지는 통째로 지워지고 새 id 로 다시 태어나 옛 메모가
갈 곳이 없었다. 이제 없어질 사람과 들어올 사람을 `subtreeDiff.js` 의 짝짓기
(`matchByPath` — 회원 ID 우선, 아니면 좌/우 자리 + `looksLikeSamePerson`)로 견줘,
같은 사람이면 그 메모도 함께 합친다. **알림에 적히는 '같은 사람' 과 메모가 실제로 합쳐지는
'같은 사람' 이 같은 셈법이어야** 스낵바에 적힌 말과 벌어진 일이 어긋나지 않는다.

파일 쪽에 이름도 회원 ID 도 없으면 **자리만 보고 같은 사람으로 친다** — 위 견주기 규칙의
'한쪽이 비어 있으면 뒤늦게 채운 것으로 본다' 가 그대로 적용된다. 알림에서만 쓰이던 어림이
이제 메모를 실제로 옮기므로 테스트가 이 방향을 못 박고 있다.

짝짓기 범위는 반드시 **없어질 사람(`doomed`)으로 좁힌다.** 1차(회원 ID)는 자리를 안 보고
전역으로 짝짓기 때문에, 안 좁히면 손대지도 않는 반대쪽 레그의 회원이 회원 ID 가 같다는
이유로 자기 메모를 넘겨주고는 자신도 그대로 남아 **같은 메모가 두 사람에게 겹쳐 적힌다.**

### 한쪽 라인만 갈아 끼우기 (`graftSubtree` 의 `side`)
파일을 고르면 `모두` / `좌라인만` / `우라인만` / `취소` 를 묻는다
(`ImportConfirmDialog.jsx`). 한쪽만 고르면 **반대쪽은 `doomed` 에 넣지 않아** id 까지
그대로 살아남는다 — 삭제도 교체도 없다. 파일에서도 그쪽 레그만 떼어 온다(`sideDescendants`).

- 파일에 그쪽이 비어 있으면 **내 그쪽도 비워진다**(파일이 이긴다). 엔진은 이 규칙을 그대로
  밀어붙이고, 대신 **확인창이 그 버튼을 막는다** — 파일 쪽 인원이 0명이면 `disabled`.
  실수로 잃는 일은 화면에서 막고 엔진은 단순하게 두는 편이 시험하기 쉽다.
- 접합점에는 `vacated: false` 를 **명시적으로 찍는다.** `{...NODE_DEFAULTS, ...n}` 이
  파일 루트를 그대로 펼치는데 `NODE_DEFAULTS` 에 `vacated` 가 없어, 파일이 빈 자리 표시를
  달고 있으면 그대로 물려받는다. 한쪽만 고르면 자식이 2 → 1 로 줄어 `collapseVacated` 가
  **방금 꽂은 접합점을 도로 걷어 간다.**

확인창은 이 저장소에서 **유일한 전체화면 모달**(`fixed inset-0`)이다. 다른 떠 있는 것들은
전부 카드에 붙는 `absolute` 팝오버지만, 되돌리기로만 물릴 수 있는 결정을 묻는 동안 나머지
화면을 만질 수 있게 두고 싶지 않고, 팬/줌으로 카드가 화면 밖에 나가 있어도 PC·모바일에서
똑같이 보여야 하기 때문이다. 버튼은 `NodeEditorPopover` 의 닫기 버튼과 같은 해법을 쓴다 —
손가락으로 누르기 힘든 문제는 글자를 키워서가 아니라 **폭을 꽉 채우고 여백을 주어** 푼다.

`App.jsx` 는 확인창에 **계보도 스냅샷을 들려 보내지 않는다.** 묻는 사이에 계보도가 바뀌면
낡은 배열로 덮어써 그 편집이 조용히 사라진다 — 버튼을 누르는 순간 `state.nodes` 를 새로 읽는다.

**id 는 파일 것을 전부 새로 발급한다.** A 가 예전에 내 파일을 받아 고친 것이라면 id 가
글자 그대로 같을 수 있고, 그대로 두면 `parentId` 가 엉뚱한 회원을 가리켜 계보도가 무너진다.
`graftSubtree` 가 `makeId` 를 **인자로 받는** 이유도 이것이다 — 테스트에서 예측 가능한
id 를 넣어 검사한다.

검증(`validateLineageFile`)에 걸리면 **배열은 한 글자도 안 바뀐다**: 루트가 하나인가 ·
끊긴 `parentId` 없는가 · 순환/고아 없는가 · 한 자리에 둘 없는가 · `side` 값이 맞는가.
기존 '열기'(통째로 바꾸기)보다 엄격한 이유는 남의 파일을 계보도 **한가운데**에 꽂기 때문이다.

파일의 `period`·`ui` 는 **안 가져온다** — 보름 기간은 내 계획이다. 다만 파일 기간이
내 화면과 다르면 확인창에서 짚어 준다.

### 이식 결과 알림 (`ImportSummaryBar.jsx` · `subtreeDiff.js`)
갈아 끼운 **그 회원 카드 바로 밑**에 **변경 / 추가 / 삭제**를 정리해 띄운다.
화면 아래(`fixed bottom`)에 두었더니 계보도에 가려 안 보여서 옮겼다 — 팬 레이어 **안**에
있어 화면을 끌면 같이 따라온다. 자리는 `OrgTreePanel` 의 `TreeNode` 가 잡고
(`importSummary.nodeId === node.id`), 편집창과 같은 자리에 뜨지만 `absolute` 라
**계보도 배치를 밀어내지 않는다** — 하위가 아래로 밀리면 견주기가 더 어려워진다.

**스스로 사라지지 않는다** — 되돌릴지 정하는 근거라 `✕ 닫기` 를 누르거나 다음 이식
때까지 남는다. 이식 직후에는 **펼친 채로** 뜬다(그러라고 띄운 것이다).
인쇄·그림에는 안 나온다(`no-print`).

읽기 쉬우려면 **한 줄에 한 칸**이어야 한다 — 여러 칸을 이어 붙이면 어디서 끊기는지
알 수 없다(`이름표 52px │ 이전 → 이후` 두 칸 격자). 직급은 카드와 **같은 색**
(`RANK_COLORS`)의 알약으로 그려 눈이 같은 것을 찾게 한다.

`data-no-pan` 이 붙어 있어 알림 위에서 끌어도 화면이 안 움직인다. 여기에 더해
**`usePanZoom` 의 휠 처리도 `[data-no-pan]` 안에서는 손대지 않는다** — 안 그러면
`preventDefault` 때문에 목록이 안 굴러가고 계보도가 대신 움직인다.

이식하면서 id 를 전부 새로 발급하므로 **id 로는 같은 사람을 못 찾는다.** 그래서 세 단계로 짝짓는다
(`matchByPath` — 이식의 메모 합치기도 이 함수를 그대로 쓴다). **접합점(경로 `''`)은 이 세 단계보다
먼저, 무조건 서로 짝짓는다** — 안 그러면 이름/회원 ID 가 우연히 접합점과 같은 엉뚱한 하위 사람이
접합점 자리를 가로챌 수 있다. 그 다음이 세 단계:
1. **회원 ID** — 양쪽에 있고 각각 한 명뿐일 때만. 자리를 옮겨도 따라가 '자리 이동'으로 잡힌다.
2. **이름** — 회원 ID 로 못 짝지은 사람 중, 양쪽에 그 이름이 **딱 한 번씩만** 나오면 짝으로 본다.
   이것도 자리를 옮겨도 따라간다. **불러오는 파일의 좌/우 배치가 내 것과 다른 게 흔해서** —
   이름이 같은데 자리만 보고 다른 사람으로 오인하면 그 사람 메모가 파일 것으로 통째로 덮인다
   (실제로 그렇게 보고됐다). 동명이인이 양쪽에 둘 이상이면 이 단계는 포기하고 3차로 넘긴다.
3. **자리(좌/우 경로)** — 그래도 남은 사람끼리(이름·회원 ID 가 둘 다 없거나 동명이인인 경우).

**자리가 같다고 같은 사람이 아니다.** 옛 좌 하위가 '나1' 인데 새 좌 하위가 'B' 라면 나1 이
B 로 '변경'된 게 아니라 나1 이 빠지고 B 가 들어온 것이다. 그래서 2·3차 모두 이름이나 회원 ID 가
양쪽 다 있는데 서로 다르면 **짝짓지 않는다**(`looksLikeSamePerson`). 한쪽이 비어 있으면 뒤늦게
채운 것으로 보고 짝짓는다.

메모는 덮어쓰는 게 아니라 합치므로 `있음 → 있음` 이 아니라 `합쳐짐`/`새로 들어옴` 으로 적는다.

> 검증: `node src/engine/subtreeImport.test.js` (91건). **id 가 통째로 겹치는 파일**이
> 가장 중요한 케이스다 — 이게 깨지면 남의 계보도를 불러올 때 내 계보도가 무너진다.
> 한쪽 라인만 이식할 때는 **반대쪽이 원래 id 그대로 살아남는가**가, 이름 짝짓기는
> **자리가 달라도 메모가 합쳐지는가**가 핵심 단언이다.

### 되돌리기 · 다시 실행 (좌 패널 플로팅 버튼 두 개)
`App.jsx` 가 손대기 **직전** 상태(`{nodes, period}` 통째로)를 `history` 에 쌓고,
버튼이 한 단계씩 돌려놓는다. 최대 50단계, 메모리에만 있고 저장하지 않는다.
되돌린 상태는 `future` 로 넘어가고 `다시 실행` 이 도로 꺼낸다 (`restoreSnapshot` 공용).
- **되돌린 뒤 새로 손을 대면 `future` 를 비운다**(`pushHistory`) — 갈라진 앞길을
  둘 다 들고 있을 수 없다. 이걸 빼먹으면 다시 실행이 엉뚱한 상태를 불러온다.
- 쌓는 곳은 `setState` **밖**이다 — `setState(prev => …)` 안에서 쌓으면
  StrictMode 가 갱신 함수를 두 번 불러 같은 상태가 두 번 들어간다.
- 이름·ID·PV·메모(`TYPED_FIELDS`)는 한 글자마다 갱신되므로 **같은 회원의 같은 칸**을
  이어 고치는 동안 한 단계로 묶는다(`coalesceKey`). 직급 고르기는 묶지 않는다.
  묶임은 편집창을 닫을 때 끊긴다(`onEndEdit`) — 다시 열어 고치면 새 단계가 쌓인다.
- 버튼은 팬 레이어 **밖**에 `absolute` 로 떠 있어 화면을 끌어도 제자리다.
  카드가 hover 때 `z-[500]` 이라 버튼은 `z-[600]`, 인쇄·그림에는 안 나온다(`no-print`).

### '나' 버튼 — 최상단 회원을 화면 맨 위 가운데로 (`resetView`)
두 패널 머리줄의 `🎯 나`. 두 가지를 지켜야 한다:

1. **재는 것이 먼저다.** `measureContent()` 는 화면 사각형에서 레이어 좌표를 되계산하는데
   그 원점에 `s.panX/panY/zoom` 을 쓴다. 팬 값을 **먼저** 0 으로 만들면 DOM 에는 아직
   옛 변형이 걸려 있어 원점이 어긋난 사각형이 박히고, 그 값으로 가두니 엉뚱한 곳에 멈췄다.
   **이 순서를 뒤집으면 다시 깨진다.**
2. **팬을 0 으로 두는 것만으로는 안 보인다.** 이진 트리라 루트가 레이어 **가운데**에 있다.
   그래서 루트 카드를 직접 찾아(`[data-tree-root]` 덩어리 안의 첫 `[data-tree-node]`)
   가로는 칸 가운데, 세로는 `PAN_EDGE_GAP` 만큼 띄운 자리로 팬을 잡는다.

**두 패널의 최상단 회원 덩어리에 `data-tree-root` 가 붙어 있어야 한다** — 떼면 예전처럼
팬만 0 으로 돌아가 루트가 화면 밖에 남는다.
(계보도가 칸보다 좁으면 `clampPan` 이 왼쪽 붙임으로 덮어쓴다 — 그래도 루트는 보인다)

### 팬 범위 제한 (`usePanZoom.js`)
빈 바탕이 `PAN_EDGE_GAP`(30px) 넘게 보이지 않도록 네 방향 모두 가둔다 — 무한 스크롤 방지.
회원 덩어리(`[data-tree-unit]`)들의 화면 사각형을 합쳐 재는데, 팬 레이어 자체를 재면
빈 레인까지 딸려와 실제 카드보다 훨씬 넓게 나오기 때문이다.
**두 패널의 노드 wrapper 에 `data-tree-unit` 이 붙어 있어야 한다** — 떼면 제한이 풀린다.
다시 재는 때: 제스처 시작 · 계보도 변경(`usePanZoom(nodes)`) · 칸 크기 변경(ResizeObserver
— 창 크기와 패널 비율 조절을 함께 잡는다) · `resetView`.
**인쇄·그림 저장 중에는 재지도 가두지도 않는다**(`isCapturing()`). 그때는 CSS 가 패널을
원본 크기로 펼치고 팬 변형을 지우므로, 그 상태에서 재면 화면과 다른 값이 박힌다.

### 두 패널 비율 조절
가로 배치는 너비(`splitPctX` → `--split-w`), 세로 배치는 높이(`splitPct` → 인라인 height)로
나뉘고 기준선도 각각 따로다(`.split-divider-v` / `.split-divider`). 어느 배치인지는
`index.css` 의 미디어쿼리 한 곳만 판단한다 — JS 로 화면 폭을 재지 않는다.
`.split-divider-v` 의 기본값 `display:none` 은 반드시 그 미디어쿼리 **앞**에 둘 것.

## 계보도의 3가지 분류
| 분류 | 표현 방식 |
|------|-----------|
| 1. 실질적으로 나와 직급을 맞추고 있는 사업자 | 직급(SSM~IM) + `status: 'active'` |
| 2. 앞으로 나와 직급을 맞추게 될 예비 사업자 | 직급(SSM~IM) + `status: 'prospect'` |
| 3. 내가 신경써야 할 소비자 | `rank: 'CSM'` (직급자 아님) |
| (미지정) | `rank: 'NONE'` — 아직 명목 직급을 안 정한 자리 |

> `status`(실질/예비)는 **화면에서 완전히 빠졌다.** 고르는 칸도, 카드 뱃지도 없다.
> 값은 `makeNode` 가 여전히 `active` 로 넣고 저장 파일에도 남지만 아무 데도 쓰이지
> 않는다. 되살리려면 편집창의 선택 칸과 카드 표시를 함께 되돌려야 한다.
> (카드의 뱃지 자리는 이제 '명목' / '목표' 가 쓴다 — 아래 참고)

## 기술 스택
- React + Vite, Tailwind CSS
- 상태: `App.jsx` 의 useState + localStorage 자동 저장
- 엔진 테스트: `node src/engine/rankEngine.test.js` · `node src/engine/subtreeImport.test.js`

## 핵심 도메인 규칙

### 직급 체계 (낮은 순)
SSM → SM → DM → SRM → STM → RM → CM → IM

`NONE`(없음, 레벨 -1) 과 `CSM`(소비자, 레벨 0) 은 직급이 아니다. SSM(1) 미만이라
어떤 자격 판정에도 포함되지 않는다. 다만 레그 카운팅은 이런 노드를 **건너뛰고 그 아래까지
계속 내려가므로**, 중간에 소비자나 미지정 자리가 있어도 하위 직급자는 그대로 집계된다.

### 달성 조건
- **SSM**: 보름 소실적 좌/우 각 150만 PV
- **SM**: 보름 소실적 좌/우 각 250만 PV
- **DM 이상**: 좌 레그에 직전 직급 2명 이상 **AND** 우 레그에 직전 직급 2명 이상
  - DM ← SM 2/2, SRM ← DM 2/2, STM ← SRM 2/2, RM ← STM 2/2, CM ← RM 2/2, IM ← CM 2/2

### SSM/SM 을 어떻게 판정하는가 (중요)
좌/우 PV 를 입력받지 않는다. 이 앱은 **구조를 짜는 도구**이고, 좌/우 실적은 하위 계보도에서
나오는 값이라 사용자가 직접 칠 값이 아니라고 보았다.
따라서 `meetsPvRank` 는 **"그 사람을 그 직급으로 보내기로 정했는가"**(`node.rank` 레벨)로 판정한다.
- SM 으로 지정 → SM 조건(좌/우 각 250만)을 채운다는 전제
- DM 이상으로 지정했는데 구조가 안 되면 → 실질은 SM 으로 내려앉는다 (등급 하락)
- 회원PV(몸PV)는 기록만 하고 달성 판정에 쓰지 않는다

좌/우 PV 검증을 되살리려면 `meetsPvRank` 와 `analyzeGap` 의 pv 분기만 고치면 된다.

### 레그 카운팅 (`countQualifiersInLeg`) — 이 앱의 심장
두 규칙이 **동시에** 적용된다:
1. **롤업**: 상위 직급자는 하위 직급 자격도 만족 (`RANK_LEVEL[실질] >= RANK_LEVEL[필요]`)
2. **자격자를 만나도 그 아래로 계속 내려가며 센다** — 같은 라인 안에 중첩 가능

이 조합이 문서상 필요 인원(STM = SRM 4 / DM 12 / SM 36)을 만들어낸다.
상위 직급자가 자기 부모의 하위 직급 자리를 겸하므로 4배가 아니라 **3배씩** 늘어난다.
`rankEngine.test.js` 가 STM 52명 / RM 160명 / CM 484명 / IM 1,456명을 검증한다.
**이 테스트가 깨지면 카운팅 규칙이 틀린 것이다.**

## 절대 규칙 (코딩 시 반드시 준수)
- **DM 이상 직급자는 몸PV로 직급 달성 불가.** 회원PV(몸PV) 입력란은 달성 판정에
  쓰지 않는다 (기록 목적).
  표시 여부는 **명목 직급**이 기준이다 — `hasMemberPv(nominalRank)`,
  명목 직급이 SRM 미만(없음/SSM/SM/DM)이면 보이고 SRM 이상이면 숨긴다.
  목표 직급과는 무관하다: 명목 SM 인데 목표를 IM 으로 잡아도 PV 칸은 보인다.
  (예전엔 목표 직급 SSM/SM/DM 기준이었다 — 헷갈리지 않도록 주의)
- CSM(소비자)·NONE(없음)은 어떤 직급 자격에도 카운트되지 않는다.
- 오른쪽 '목표 계보도' 카드에는 **달성할 직급(목표)** 만 이름·색으로 적는다.
  조건 미달이어도 내려앉히지 않는다 — 목표와 결과의 차이는 카드 아래 부족분
  문구가 말해 준다. 실질 직급을 카드에 적으면 안 된다. 명목 직급도 마찬가지다.
- 명목 직급은 계산 로직에 절대 넣지 않는다.
- 실질 직급은 하위에만 의존하므로 아래에서 위로 한 번에 확정된다 (순환 없음).

## 구조
```
src/engine/ranks.js          직급 정의 · 달성 조건 테이블 · 색상
src/engine/rankEngine.js     실질 직급 계산 · 레그 카운팅 · 부족분 분석
src/engine/rankEngine.test.js 문서 인원 수치 대조 검증
src/engine/subtreeImport.js  계보도 이식 — 파일 검증 · id 재발급 · 접합 · 빈 자리 정리
src/engine/subtreeDiff.js    이식 전후 견주기 (변경/추가/삭제) — 스낵바가 읽는 값
src/engine/subtreeImport.test.js 이식·견주기 검증 (id 충돌 포함)
src/components/OrgTreePanel.jsx      좌: 나의 계보도 (카드 터치 → 직급 선택)
src/components/EffectiveTreePanel.jsx 우: 목표 계보도 (좌측에서 파생, 자동 반영)
src/components/NodeEditorPopover.jsx 이름/ID → 명목 직급(접힘) → 목표 직급 → PV → 메모
src/components/RankQuickPicker.jsx   카드의 '목표' 상자를 누르면 뜨는 직급 목록
src/components/keyboard.js           단축키(esc · ` 1~8 · Q/W) + 글자 칸 판별
src/components/TopBar.jsx            맨 위 줄 — 기간(년·월·반기)을 한 줄에
src/components/NumberField.jsx       PV 입력칸 (0 이면 포커스 때 비운다)
src/components/CaptureCaption.jsx    인쇄·그림에만 찍히는 대상 기간 머리글
src/components/ImportSummaryBar.jsx  이식 결과 알림 — 변경/추가/삭제 (스스로 안 사라진다)
src/components/ImportConfirmDialog.jsx 이식 전 확인창 — 모두/좌라인만/우라인만/취소 (유일한 전체화면 모달)
src/components/TreePanelHeader.jsx   두 패널 공용 머리줄 (제목 + 메뉴 + 요약 접기)
src/components/CopyableId.jsx        회원 ID + 복사 아이콘 (clipboard API, execCommand 폴백)
src/components/usePanZoom.js         팬 + 휠줌 + 핀치줌 (두 패널 공용)
src/components/usePanelCapture.js    인쇄 / 그림(JPG) 저장 — 누른 패널만 대상
src/components/treeLayout.js         이진 트리 폭 계산 (두 패널 공용)
```

## 미구현 / 알려진 범위
- 반기(상/하반기) 선택은 계획의 **라벨**이다. 날짜별 PV 배분 그리드는 없다.
  PV 입력값은 보름 소실적 합계 기준.
- 후원수당 금액 계산 없음. 직급 달성 구조 설계에만 집중.
- CSM 의 예상 소비 PV 는 입력·보관되지만 상위 노드 PV 로 자동 롤업되지 않는다.
